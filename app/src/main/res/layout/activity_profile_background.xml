<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".ProfileImageActivity">

    <!-- 닫기 버튼 (왼쪽 상단) -->
<ImageView
android:id="@+id/imageViewClose"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:src="@drawable/ic_close_white"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:padding="16dp"
    android:clickable="true"
    android:focusable="true"
    android:contentDescription="닫기 버튼" />

    <!-- 갤러리/썸네일 버튼 (오른쪽 상단) -->
<ImageView
android:id="@+id/imageViewGalleryThumbnail"
android:layout_width="48dp"
android:layout_height="48dp"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:padding="16dp"
    android:clickable="true"
    android:focusable="true"
    android:contentDescription="갤러리 썸네일" />

    <!-- 프로필 상세 이미지를 표시하고 좌우 스와이프를 지원하는 ViewPager2 -->
<androidx.viewpager2.widget.ViewPager2
android:id="@+id/viewPagerProfileImages"
android:layout_width="match_parent"
android:layout_height="590dp"
app:layout_constraintTop_toBottomOf="@+id/imageViewGalleryThumbnail"
app:layout_constraintBottom_toTopOf="@+id/textViewImageCounter"
app:layout_constraintStart_toStartOf="parent"
app:layout_constraintEnd_toEndOf="parent" />

    <!-- 현재 이미지 인덱스를 표시하는 텍스트 뷰 -->
<TextView
android:id="@+id/textViewImageCounter"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:textColor="@android:color/white"
android:textSize="16sp"
android:padding="8dp"
app:layout_constraintBottom_toBottomOf="parent"
app:layout_constraintEnd_toEndOf="parent"
app:layout_constraintStart_toStartOf="parent"
android:layout_marginBottom="16dp"
tools:text="1 / 5" />

    </androidx.constraintlayout.widget.ConstraintLayout>
